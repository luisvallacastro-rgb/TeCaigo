{
  "name": "tecaigo-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend inicial de TeCaiGO para eventos, registros y preparacion de produccion.",
  "main": "server.cjs",
  "scripts": {
    "start": "node server.cjs",
    "dev": "node server.cjs",
    "check": "node --check server.cjs && node --check db.cjs && node --check db-postgres.cjs && node --check database.cjs && node --check env.cjs"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
