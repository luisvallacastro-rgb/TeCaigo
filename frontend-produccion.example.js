// Copiar este contenido a config.js cuando el backend ya este desplegado.
// Cambiar la URL por la URL real del servidor backend.

window.TECAIGO_CONFIG = {
  API_BASE_URL: "https://api.tecaigo.com/api",
};
